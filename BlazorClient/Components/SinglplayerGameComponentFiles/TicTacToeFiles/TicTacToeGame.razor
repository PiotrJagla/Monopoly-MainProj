@inherits TicTacToeGameBase

<div class="board">
    @for (int y = 0; y < Consts.TicTacToe.BoardSize.y; y++)
    {
        for (int x = 0; x < Consts.TicTacToe.BoardSize.x; x++)
        {
            Point2D point = new Point2D(x, y);
            <button disabled="@(UserMessage!="")" @onclick="() => OnUserBoardClick(point)" class="TicTacToeButton">
                <label class="ButtonText">@TicTacToeLogic.GetPoint(point)</label>
            </button>
        }
    }
</div>

<style>
    .board{
        display: grid;
        grid-template-columns: repeat(@Consts.TicTacToe.BoardSize.x, 1fr);
        grid-template-columns: repeat(@Consts.TicTacToe.BoardSize.x, 100px);
    }
</style>

<button disabled="@(UserMessage=="")" class="btn btn-primary" @onclick="PlayAgain">Restart</button>

<h1>@UserMessage</h1>

